# OpenRouter — LLM API. Use a DEDICATED key for Heroku with a €10 hard cap
# set at the key level. Disable auto-topup at the account level.
OPENROUTER_API_KEY=sk-or-v1-...

# --- Spend caps (the real financial safety net) ---------------------------
# Checked in backend/llm.py BEFORE every request leaves the process, so the
# app stops itself with a readable message instead of walking into a
# provider-side 402.
#
# RUN_COST_CAP_USD     — per-run ceiling; the run ends with
#                        ended_reason='cost_cap_exceeded'.
# MONTHLY_COST_CAP_USD — ceiling across ALL runs in a calendar month. Keep it
#                        below your OpenRouter key-level credit cap
#                        (€10 ≈ $11), so this trips first.
# Set either to 0 to disable that cap. Defaults: 0.75 and 8.0.
RUN_COST_CAP_USD=0.75
MONTHLY_COST_CAP_USD=8.0

# Supabase Postgres connection string.
#
# OPTIONAL FOR LOCAL DEV. Leave it unset and the app runs against an
# in-process store (state lost on restart) — which is also how the offline
# test suite and scripts/dev_server.py run. REQUIRED on Heroku.
# IMPORTANT: use the POOLER endpoint, not the direct db.*.supabase.co one.
# Supabase's direct endpoint is IPv6-only on the free tier since 2024;
# Heroku dynos have no IPv6, so asyncpg fails with "Network is unreachable".
# The pooler is IPv4 and works everywhere. Get the exact string from
# Supabase Dashboard -> Project Settings -> Database -> Connection string
# -> "Transaction pooler". Note the username is postgres.<project_ref>
# (NOT just "postgres"), and the region/port are baked in.
# Format: postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres

# Admin password for the beta. The frontend login screen posts this to
# /api/login; a signed cookie is issued and checked by middleware.
ADMIN_PASSWORD=change-me

# Random hex used to sign session cookies. Generate once per environment:
#   python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change-me-to-64-random-hex-chars

# Kill switch. Set to 1 to make all /api/* except /api/health return 503.
# Flip this in the Heroku dashboard if something goes wrong — no redeploy needed.
DISABLED=

# Chance that a given fictional day gets an ambient world event (lift broken,
# water cut, noisy night). 0 disables them. Default 0.45.
WORLD_EVENT_PROBABILITY=0.45

# Set to 0 to silence per-activation traces on stderr. Default 1.
VERBOSE_LOGGING=1
